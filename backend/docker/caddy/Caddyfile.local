# Local development — HTTP only on port 80 inside the container.
{
	admin off
}

http://localhost:80 {
	handle /api/* {
		uri strip_prefix /api
		reverse_proxy api:{$API_PORT:58081}
	}

	handle {
		reverse_proxy api:{$API_PORT:58081}
	}
}
