/// Application-wide non-secret constants.
class AppConstants {
  AppConstants._();

  static const String appName = 'School Camera';
  static const String appTagline = 'Secure school camera access for parents.';

  /// Splash display duration before navigation.
  static const Duration splashDuration = Duration(milliseconds: 2200);
}
