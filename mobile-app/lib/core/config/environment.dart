/// Application deployment environments.
enum AppEnvironment {
  development,
  staging,
  production,
}

/// Default production API base URL for the School Camera platform.
const String defaultApiBaseUrl = 'https://camera.iglooks.com/api/';
