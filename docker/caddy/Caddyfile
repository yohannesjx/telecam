# Default Caddy config for docker-compose (local). For production use Caddyfile.production — see README.
{
	admin off
}

http://localhost:80 {
	handle /api/* {
		uri strip_prefix /api
		reverse_proxy api:{$API_PORT:58081}
	}

	handle {
		reverse_proxy api:{$API_PORT:58081}
	}
}
